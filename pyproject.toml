[project]
name = "weather"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Andy Armstrong", email = "andy@hexten.net" }
]
requires-python = ">=3.14"
dependencies = [
    "paho-mqtt>=2.1.0",
    "pyserial>=3.5",
]

[project.scripts]
weather = "weather:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
